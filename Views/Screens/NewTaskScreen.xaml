<UserControl x:Class="TaskManager.Views.Screens.NewTaskScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:TaskManager.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <ControlTemplate x:Key="ValidationErrorTemplate">
            <DockPanel LastChildFill="False" VerticalAlignment="Center">
                <AdornedElementPlaceholder />
                <TextBlock Foreground="Red" FontSize="12" Margin="5 0 0 0"
                           Text="{Binding [0].ErrorContent}"
                           VerticalAlignment="Center"/>
            </DockPanel>
        </ControlTemplate>

        <Style TargetType="TextBox">
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ValidationErrorTemplate}" />
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="Background" Value="#FFFDECEA"/>
                    <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ValidationErrorTemplate}" />
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="Background" Value="#FFFDECEA"/>
                    <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:NewTaskScreenViewModel />
    </UserControl.DataContext>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề chính, căn trái -->
        <TextBlock Text="Create New Task" FontSize="28" FontWeight="Bold" Margin="0 0 0 20" HorizontalAlignment="Left"/>

        <!-- Form -->
        <StackPanel Grid.Row="1" Width="450" HorizontalAlignment="Left">
            <!-- Code -->
            <TextBlock Text="Code:" FontSize="14" Margin="0 0 0 2" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding Code, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Margin="0 0 0 10"/>

            <!-- Title -->
            <TextBlock Text="Title:" FontSize="14" Margin="0 0 0 2" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Margin="0 0 0 10"/>

            <!-- Description -->
            <TextBlock Text="Description:" FontSize="14" Margin="0 0 0 2" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" 
                     Height="80" TextWrapping="Wrap" Margin="0 0 0 10"/>

            <!-- Status -->
            <TextBlock Text="Status:" FontSize="14" Margin="0 0 0 2" HorizontalAlignment="Left"/>
            <ComboBox SelectedValue="{Binding Status, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" 
                      SelectedValuePath="Tag" Margin="0 0 0 10">
                <ComboBoxItem Content="Not Started" Tag="0"/>
                <ComboBoxItem Content="In Progress" Tag="1"/>
                <ComboBoxItem Content="Completed" Tag="2"/>
            </ComboBox>

            <!-- Due Date -->
            <TextBlock Text="Due Date:" FontSize="14" Margin="0 0 0 2" HorizontalAlignment="Left"/>
            <DatePicker SelectedDate="{Binding DueDate, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 10"/>

            <!-- Priority -->
            <TextBlock Text="Priority:" FontSize="14" Margin="0 0 0 2" HorizontalAlignment="Left"/>
            <ComboBox SelectedValue="{Binding Priority, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" 
                      SelectedValuePath="Tag" Margin="0 0 0 10">
                <ComboBoxItem Content="Low" Tag="2"/>
                <ComboBoxItem Content="Medium" Tag="1"/>
                <ComboBoxItem Content="High" Tag="0"/>
            </ComboBox>

            <!-- Reporter Id -->
            <TextBlock Text="Reporter Id:" FontSize="14" Margin="0 0 0 2" HorizontalAlignment="Left"/>
            <ComboBox ItemsSource="{Binding ReporterUsers}" 
                      SelectedItem="{Binding ReporterDisplayName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" 
                      IsEditable="False" Margin="0 0 0 10"/>

            <!-- Assignee Id -->
            <TextBlock Text="Assignee Id:" FontSize="14" Margin="0 0 0 2" HorizontalAlignment="Left"/>
            <ComboBox ItemsSource="{Binding AssigneeUsers}" 
                      SelectedItem="{Binding AssigneeDisplayName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" 
                      IsEditable="False" Margin="0 0 0 10"/>
        </StackPanel>

        <!-- Nút lưu hủy -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 20 0 0">
            <Button Content="Save" Width="100" Margin="10 0" Command="{Binding SaveCommand}"/>
            <Button Content="Cancel" Width="100" Command="{Binding ClearCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
