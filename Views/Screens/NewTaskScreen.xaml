<UserControl x:Class="TaskManager.Views.Screens.NewTaskScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:TaskManager.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:NewTaskScreenViewModel />
    </UserControl.DataContext>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Create New Task" FontSize="24" FontWeight="Bold" Margin="0 0 0 20"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center" Width="400">
            <TextBlock Text="Code"/>
            <TextBox Text="{Binding Code, UpdateSourceTrigger=PropertyChanged}" Margin="0 5"/>

            <TextBlock Text="Title"/>
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0 5"/>

            <TextBlock Text="Description"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="80" TextWrapping="Wrap" Margin="0 5"/>

            <TextBlock Text="Status"/>
            <ComboBox SelectedValue="{Binding Status, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Tag" Margin="0 5">
                <ComboBoxItem Content="Not Started" Tag="0"/>
                <ComboBoxItem Content="In Progress" Tag="1"/>
                <ComboBoxItem Content="Completed" Tag="2"/>
            </ComboBox>

            <TextBlock Text="Due Date"/>
            <DatePicker SelectedDate="{Binding DueDate, UpdateSourceTrigger=PropertyChanged}" Margin="0 5"/>

            <TextBlock Text="Priority"/>
            <ComboBox SelectedValue="{Binding Priority, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Tag" Margin="0 5">
                <ComboBoxItem Content="Low" Tag="2"/>
                <ComboBoxItem Content="Medium" Tag="1"/>
                <ComboBoxItem Content="High" Tag="0"/>
            </ComboBox>

            <TextBlock Text="Reporter Id"/>
            <ComboBox ItemsSource="{Binding ReporterUsers}" 
                      SelectedItem="{Binding ReporterId, UpdateSourceTrigger=PropertyChanged}" 
                      IsEditable="False" Margin="0 5"/>

            <TextBlock Text="Assignee Id"/>
            <ComboBox ItemsSource="{Binding AssigneeUsers}" 
                      SelectedItem="{Binding AssigneeId, UpdateSourceTrigger=PropertyChanged}" 
                      IsEditable="False" Margin="0 5"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 20 0 0">
            <Button Content="Save" Width="100" Margin="10 0" Command="{Binding SaveCommand }"/>
            <Button Content="Clear" Width="100" Command="{Binding ClearCommand }"/>
        </StackPanel>
    </Grid>
</UserControl>
