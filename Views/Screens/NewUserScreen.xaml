<UserControl x:Class="TaskManager.Views.Screens.NewUserScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:TaskManager.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <!-- Validation Template -->
    <UserControl.Resources>
        <ControlTemplate x:Key="ValidationErrorTemplate">
            <Grid VerticalAlignment="Center" SnapsToDevicePixels="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" BorderBrush="Red" BorderThickness="1">
                    <AdornedElementPlaceholder HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="-1"/>
                </Border>
                <TextBlock Grid.Column="1"
                           Foreground="Red"
                           FontSize="12"
                           VerticalAlignment="Center"
                           Text="{Binding [0].ErrorContent}"
                           Margin="5,0,0,0"/>
            </Grid>
        </ControlTemplate>
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:NewUserViewModel />
    </UserControl.DataContext>

    <Grid Margin="20" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MinHeight="600">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Create New User"
                   FontSize="30"
                   FontWeight="Bold"
                   Margin="0 0 0 20"
                   HorizontalAlignment="Left"/>

        <!-- Scrollable Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Width="400">

                <TextBlock Text="Employee Code:"/>
                <TextBox Text="{Binding EmployeeCode, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}"
                         Validation.ErrorTemplate="{StaticResource ValidationErrorTemplate}" Margin="0 0 0 8"/>

                <TextBlock Text="Username:"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}"
                         Validation.ErrorTemplate="{StaticResource ValidationErrorTemplate}" Margin="0 0 0 8"/>

                <TextBlock Text="Password:"/>
                <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}"
                         Validation.ErrorTemplate="{StaticResource ValidationErrorTemplate}" Margin="0 0 0 8"/>

                <TextBlock Text="Display Name:"/>
                <TextBox Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 8"/>

                <TextBlock Text="Email:"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 8"/>

                <CheckBox Content="Is Admin"
                          IsChecked="{Binding IsAdmin}"
                          Margin="0 10 0 8"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 20 0 0">
            <Button Content="Save" Width="100" Margin="5" Command="{Binding SaveCommand}"/>
            <Button Content="Clear" Width="100" Margin="5" Command="{Binding ClearCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
